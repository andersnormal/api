syntax = "proto3";

import "google/firestore/v1beta1/write.proto";

package andersnormal.newton.store;

service Store {
    rpc SaveDialog(DialogRequest) returns (google.firestore.v1beta1.WriteResult) {}
}

message DialogRequest {
    int32 customer = 1;
}